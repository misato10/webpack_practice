doctype html

block locales
  - var title = 'My website';

html(lang="en") 
  head 
    meta(charaset="UTF-8") 
    mata(name="viewport" content="width=device-width, initial-scale=1.0") 
    title #{title}
  body
    include  _menu.pug
    block content